package com.briup.web.Servlet;
import java.io.IOException;
import java.text.SimpleDateFormat;
import java.util.ArrayList;

import javax.servlet.ServletContext;
import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import com.briup.bean.Notice;
@WebServlet("/Noticeid")
public class Noticeid extends HttpServlet {
	private static final long serialVersionUID = 1L;
	protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		   // TODO Auto-generated method stub
		 /*  int id=Integer.parseInt(request.getParameter("param"));
	        ServletContext app= request.getServletContext();
	        @SuppressWarnings("unchecked")
			ArrayList<Notice>  lists= (ArrayList<Notice>) app.getAttribute("noti");
	        Notice noti=null;
	        for(Notice r:lists){
				if(id==r.getId()){
					noti=r;
				}
			}	
	        SimpleDateFormat sdf=new SimpleDateFormat("yyyy-MM-dd HH:mm:ss");
	        String time= sdf.format(noti.getTime());
	        request.setAttribute("time", time);
	        request.setAttribute("noti", noti);
		    request.getRequestDispatcher("/user/main/listnotice.jsp").forward(request, response);	}*/
	}
	protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		// TODO Auto-generated method stub
		doGet(request, response);
		}

}
